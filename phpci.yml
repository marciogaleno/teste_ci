build_settings:
    ignore:
        - "vendor"
        - "tests"

test:
    php_unit:
        config:
            - "phpunit.xml"
        directory:
            - "tests/"
    php_loc:
        directory: "src"
